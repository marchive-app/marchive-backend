spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/marchive
    username: root
    password: 1234

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

jwt:
  secret: local-test-secret-key-must-be-32-bytes-or-longer!!

google:
  client-id: local-test-client-id

qdrant:
  host: localhost
  port: 6334