spring:
  datasource:
    url: jdbc:mysql://localhost:3306/marchive?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: ${DEV_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
jwt:
  secret: ${DEV_JWT_SECRET}
qdrant:
  host: ${DB_HOST}
  port: 6333